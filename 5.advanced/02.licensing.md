---
title: Licensing
description: Create and manage dxflow licenses and understand the available permissions
navigation:
    icon: i-hugeicons:license
---

dxflow uses a permission-based licensing system to control access to features. Licenses are JSON files signed with RSA, verified at engine startup against an embedded public key: a valid license starts the engine, an invalid one stops it, and a missing license falls back to the built-in General license. No custom license is required to start.

## Default license (General)

The built-in General License needs no registration and is valid until **January 1, 2030**. It grants the core modules `shell`, `artifact`, `platform`, and `workflow`. The `proxy` and `bridge` modules require a signed license.

The `auth` and `engine` controllers (and `console` when started with `--console`) are always served regardless of license.

## Permissions

| Module | Description |
| --- | --- |
| `shell` | Terminal access and command execution |
| `artifact` | File storage and management |
| `platform` | Docker and platform integrations |
| `workflow` | Compose workflow orchestration |
| `bridge` | Network bridge connections (with traffic limits) |
| `proxy` | Proxy configurations and tunnels (with traffic limits) |

## Getting your identity

Licenses are tied to your machine's unique Identity and cannot be transferred between machines. Get it with:

```bash
dxflow engine info
```

```
Identity:    cc2bcf4a9fc3fa056189533df3ee3af6
Directory:   /home/user/.dxflow
Profile:     default
```

## License commands

| Command | Description |
| --- | --- |
| `dxflow license info` | View active license details |
| `dxflow license create` | Create a license via the browser portal (alias: `license issue`) |
| `dxflow license activate <IDENTITY>` | Activate a downloaded license (alias: `license active`) |
| `dxflow license activate general` | Re-activate the built-in default |

`dxflow license create` opens your browser to the license portal with your Identity and name pre-filled, guides you through authentication, and generates the license file. Activating stores the license as `license-<identity>.json` (identity truncated to 8 chars) and updates the `license` config setting.

## Creating a custom license

::steps
### Run the create command

```bash
dxflow license create
```

Your browser opens with your details pre-filled.

### Authenticate

Choose **Google** (verified via email) or **Stripe** (verified with any payment card — no charges, the card is used only for identity verification and is not stored).

### Configure

Provide a license name. Other settings are automatic: validity 5 years (2025–2030), all core modules enabled, bridge with 1 connection and 10MB traffic, proxy disabled by default.

### Download and activate

```bash
dxflow license activate <IDENTITY>
```
::

Authentication ties each license to a verified identity to prevent abuse; no payment or personal data is collected beyond what verification needs.

## License format

```json
{
  "identity": "your-machine-identity",
  "name": "License Name",
  "owner": "Anonymous",
  "starts": "Mon, 01 Jan 2025 00:00:00 GMT",
  "expires": "Mon, 01 Jan 2030 00:00:00 GMT",
  "permission": {
    "shell": {},
    "artifact": {},
    "platform": {},
    "workflow": {},
    "bridge": {
      "count": 1,
      "traffic": 10240000
    }
  },
  "signature": "cryptographic-signature",
  "issued": "Mon, 01 Jan 2025 00:00:00 GMT"
}
```

## When to use a custom license

The General License covers evaluation, development, and general use. Create a custom license only to track usage with a personalized identity, prepare for post-2030 usage, or meet organizational compliance requirements. When a license expires, run `dxflow license create` again and re-activate. For additional capabilities or custom configurations, contact info@dxflow.ai.

## Troubleshooting

| Problem | Fix |
| --- | --- |
| License not verified | Confirm the file is unmodified, the license identity matches your machine, and the system date/time is correct |
| License expired | Check the expiration date with `dxflow license info`, then run `dxflow license create` |
| Invalid license signature | The file may be corrupted — download a fresh copy |

Check current status with `dxflow license info`, and report issues at [GitHub](https://github.com/dxflow-ai/community/issues).
