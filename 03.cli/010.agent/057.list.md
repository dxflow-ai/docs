---
title: Agent List
description: List agent sessions
---

### Usage

```bash [Terminal]
dxflow agent list
```

Alias: `dxflow agent ls`.

Lists every agent session with its identity, context workflow, latest model, and creation time.

### Example

```bash [Terminal]
dxflow agent list

dxflow agent ls
```
