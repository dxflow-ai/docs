---
title: Bridge create request
description: Request of create bridge
---

### Endpoint

```bash
POST /api/bridge/
```

### Auth

```bash
Authorization: Bearer ${token}
```

### Body

```bash
Content-Type: application/json
```

```json [Json]
{
    "gateway": "",
    "upstream": ""
}
```

### Response

```json [Json]
[
    {
        "kind": "status",
        "payload": {
            "code": 0,
            "message": ""
        }
    },
    {
        "kind": "entity",
        "payload": {
            "address": "",
            "created_at": 0,
            "gateway": "",
            "identity": "",
            "limited": false,
            "password": "",
            "pool": 0,
            "status": "",
            "traffic": {
                "gateway_read": 0,
                "gateway_write": 0,
                "upstream_read": 0,
                "upstream_write": 0
            },
            "updated_at": 0,
            "upstream": "",
            "username": ""
        }
    }
]
```

### Notes

- Bridge Permission Required
- Read-Only Permission Restricted
- `gateway` is the hub URL; when omitted it falls back to the engine's `bridge-gateway` configuration, and the request is rejected with `400` when neither is set
- `address` is the public URL the bridge is reachable on, derived from the gateway and the bridge identity
