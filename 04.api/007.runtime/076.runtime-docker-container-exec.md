---
title: Runtime docker container exec request
description: Execute a command in a runtime docker container
---

### Endpoint

```bash
PUT /api/runtime/docker/container/
```

### Auth

```bash
Authorization: Bearer ${token}
```

### Body

```bash
Content-Type: application/json
```

```json [Json]
{
    "identity": "",
    "directory": "",
    "command": []
}
```

`identity` and a non-empty `command` are required. `directory` is the optional working directory.

### Response

Each line of command output is streamed back as a string `entity` chunk.

```json [Json]
[
    {
        "kind": "status",
        "payload": {
            "code": 0,
            "message": ""
        }
    },
    {
        "kind": "entity",
        "payload": ""
    }
]
```

### Notes

- Runtime Permission Required
- Read-Only Permission Restricted
