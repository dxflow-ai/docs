---
title: Agent create request
description: Request of create agent session
---

### Endpoint

```bash
POST /api/agent/session/
```

### Auth

```bash
Authorization: Bearer ${token}
```

### Query Parameters

| Key | Default | Description |
|-----|---------|-------------|
| identity |  | Optional; auto-generated when empty |
| workflow |  | Optional; when set, it becomes the session's context workflow |

### Response

```json [Json]
[
    {
        "kind": "status",
        "payload": {
            "code": 0,
            "message": ""
        }
    },
    {
        "kind": "entity",
        "payload": {
            "identity": "",
            "workflow": "",
            "created_at": 0
        }
    }
]
```

### Notes

- Agent Permission Required
- Read-Only Permission Restricted
- A session is one conversation that can create and operate several workflows. A seeded `workflow` becomes the session's context workflow — the default for workflow-scoped prompts — but is optional.
